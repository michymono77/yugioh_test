<div class="container">
  <h1>All Decks</h1>
  <ol class="gradient-list">
    <% @decks.each do |deck| %>
      <li><%= link_to deck.name, deck_path(deck)%></li>
    <% end %>
  </ol>
    <div><%= link_to "Add a deck",  new_deck_path, class:"btn btn-outline-dark" %></div>
</div>
